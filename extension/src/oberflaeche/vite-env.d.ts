/**
 * Typen fuer `import.meta.glob` (Katalog-Buendelung in `i18n.ts`) und
 * `*.css`-Importe. `ImportMetaEnv` aus `vite/client` fliesst mit der
 * Deklaration in `src/gemeinsam/umgebung.d.ts` (B1) zusammen; beide tragen
 * denselben Namen, genau dafuer.
 */
/// <reference types="vite/client" />
